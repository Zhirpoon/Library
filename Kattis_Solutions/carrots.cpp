#include <iostream>
#include <stdio.h>

using namespace std;

int main() {
    int conts, solvs;
    if(scanf("%d %d", &conts, &solvs) != 2) {
        return 0;
    }
    cout << solvs << endl;
    return 0;
}